# Feature todo

Possible next steps:

- Add a wizard shell plugin.
- Add a tabbed shell plugin.
- Add position strategy for anchored non-modal dialogs.
- Add animation hooks.
- Add optional IIFE/browser-global build if a build step is introduced later.
- Add TypeScript declarations if the module becomes a published package.
